import 'package:flutter/material.dart';
class ChartsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: Text('Charts and Maps of Data')), body: Center(child: Text('Fill in')));
}