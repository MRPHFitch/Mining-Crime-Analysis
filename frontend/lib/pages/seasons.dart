import 'package:flutter/material.dart';
class SeasonsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: Text('Seasonal Crime Patterns')), body: Center(child: Text('Fill in')));
}