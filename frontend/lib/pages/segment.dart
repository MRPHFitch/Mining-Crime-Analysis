import 'package:flutter/material.dart';
class SegmentPage extends StatelessWidget {
  const SegmentPage({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: Text('Segment Crime on Time and Location')), body: Center(child: Text('Fill in')));
}