import 'package:flutter/material.dart';
class SequencesPage extends StatelessWidget {
  const SequencesPage({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: Text('Recurring Crime Sequences')), body: Center(child: Text('Fill in')));
}